{
  "name": "roleproof",
  "version": "0.1.0",
  "description": "Dependency-free symbolic access-policy analysis with independently verified escalation witnesses.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "engines": {
    "node": ">=22 <25"
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node tools/build-site.mjs",
    "test": "node --test",
    "check": "npm test && npm audit --omit=dev"
  }
}
